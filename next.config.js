/** @type {import('next').NextConfig} */
const nextConfig = {
    theme: {
        colors: {
            primary: '#003087',
            secondary: '#EF2C2E',
            // secondary2: '#f7f7f7',
        },
    },
};

module.exports = nextConfig
